import { ForviewChildDirective } from './forview-child.directive';

describe('ForviewChildDirective', () => {
  it('should create an instance', () => {
    const directive = new ForviewChildDirective();
    expect(directive).toBeTruthy();
  });
});
